Summary: This is the creation of a wrapper for running Playwright tests

Goal: Create a simplified user experience for running automation, simulated tests using the growing testing tool, Playwright.

Business Value: Allowing non-technical users to run tests by a push of a button rather than running console commands, thus, will reduce the need for a Playwright engineer.

How to run:
python -m uvicorn app:app --reload